{
  "name": "@tenantease/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/apps/api/src/server.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prisma:generate": "dotenv --quiet -e ../../.env -- prisma generate",
    "prisma:migrate": "dotenv --quiet -e ../../.env -- prisma migrate dev --name init",
    "prisma:seed": "dotenv --quiet -e ../../.env -- tsx prisma/seed.ts",
    "db:studio": "dotenv --quiet -e ../../.env -- prisma studio"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/jwt": "^10.1.0",
    "@fastify/multipart": "^10.0.0",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/adapter-pg": "7.8.0",
    "@prisma/client": "^7.8.0",
    "@tenantease/types": "workspace:*",
    "csv-parse": "^6.2.1",
    "dotenv": "^17.4.2",
    "fastify": "^5.8.5",
    "fastify-plugin": "^5.0.1",
    "pdf-lib": "^1.17.1",
    "pg": "8.21.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "dotenv-cli": "^11.0.0",
    "prisma": "^7.8.0",
    "tsx": "^4.19.3",
    "typescript": "^6.0.3",
    "vite": "8.0.16",
    "vitest": "4.1.8"
  }
}
